<Page x:Class="Gromi.Template.Wpf.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="RegisterView" Width="552"
        Background="{DynamicResource RegionBrush}">
    <hc:TransitioningContentControl>
        <StackPanel>
            <TextBox Width="380"
              hc:InfoElement.TitleWidth="140"
              hc:InfoElement.TitlePlacement="Left"
              hc:InfoElement.Title="用户名"
              Style="{StaticResource TextBoxExtend}"
              Margin="0,32,0,0"
              Text="{Binding Model.Name}">
            </TextBox>
            <TextBox Width="380"
              hc:InfoElement.TitleWidth="140"
              hc:InfoElement.TitlePlacement="Left"
              hc:InfoElement.Title="账号"
              Style="{StaticResource TextBoxExtend}"
              Margin="0,32,0,0"
              Text="{Binding Model.Account}">
            </TextBox>
            <TextBox Width="380"
              hc:InfoElement.TitleWidth="140"
              hc:InfoElement.TitlePlacement="Left"
              hc:InfoElement.Title="密码"
              Style="{StaticResource TextBoxExtend}"
              Margin="0,32,0,0"
              Text="{Binding Model.Password}">
            </TextBox>
            <Button Content="注册" Margin="5" Style="{StaticResource ButtonPrimary}" Command="{Binding Register}" />
        </StackPanel>
    </hc:TransitioningContentControl>
</Page>